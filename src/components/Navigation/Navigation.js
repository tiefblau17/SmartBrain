import React from 'react';

const Navigation=()=>{
	return( 
		<nav style={{display: 'flex', justifyContent: 'flex-end'}}>
			<p className='f3 link dim black underline pas pointer pa2'>Sign in</p>
		</nav>
	);
}

export default Navigation;